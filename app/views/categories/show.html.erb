<h2><%= @category.name %>'s</h2>

<% @category.cars.each do |car| %>
<div class="list">
    <h3><%= car.name %></h3>
    <br>
    <li><%= image_tag(car.image, style: "width:400px") %></li>
    <br>
    <h4>$<%= car.price %></h4>
    <br>
    <li><%= link_to 'Show', car %></li>
    <li><%= link_to 'Edit Car', edit_car_path(car) %></li>
    <br>
    <br>
</div>
<% end %>
